

export const metadata = {
    title: "LMS Subjects",
    description: "This is LMS App",
};

export default function subject({ children }) {
    return <div className="h-full  w-full">{children}</div>;
}
